name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20, 22]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run compile

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Package extension
        run: npx @vscode/vsce package --no-dependencies
        # --no-dependencies since esbuild bundles everything

      - name: Upload .vsix artifact
        if: matrix.node-version == 22
        uses: actions/upload-artifact@v4
        with:
          name: warpgate-vsix
          path: "*.vsix"
          retention-days: 30
